{
  "name": "vivenu-webhook",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "gcp-build": "npm run build",
    "deploy": "gcloud functions deploy vivenuWebhook --gen2 --runtime=nodejs22 --trigger-http --allow-unauthenticated --source=. --entry-point=vivenuWebhook --region=us-east1 --project=mercer-labs-488707 --memory=256Mi --timeout=30s"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.0",
    "@google-cloud/functions-framework": "^3.4.0",
    "@google-cloud/secret-manager": "^5.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  }
}
